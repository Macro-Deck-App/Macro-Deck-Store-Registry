# Cool CPU Load Widget

A ready-to-use history graph for monitoring CPU utilization directly on your Macro Deck surface. The
widget displays the current CPU load as a percentage and uses the processor name as its subtitle.

The template uses Macro Deck's built-in `system_cpu_usage_percent` and `system_cpu_name` variables, so
no external service or additional plugin is required.
